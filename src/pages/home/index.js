import React from 'react';
import Header from '@/components/header'

function index(props) {
    return (
        <div style={{
            width: '100%'
        }}>
            <Header/>
        </div>
    );
}

export default index;